<!-- Pages/StartPage.xaml -->
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             x:Class="ItalianBrainrotClicker.Pages.StartPage"
             Title="Start">
    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="20">
        <Label Text="ITALIAN BRAINROT CLICKER" FontSize="32" HorizontalOptions="Center" />
        <Button Text="Start" Clicked="OnStartClicked" FontSize="24" />
    </VerticalStackLayout>
</ContentPage>
